name: Weather Monitoring System - Complete Automation

# ═══════════════════════════════════════════════════════════════════════════════
# FIXED: This workflow now properly runs BOTH monitoring systems
#
# 1. REAL-TIME ALERTS - Runs every 15 minutes
# 2. HISTORICAL SUMMARIES - Runs every 6 hours
#
# The key fix: Removed the broken github.event.schedule checks
# ═══════════════════════════════════════════════════════════════════════════════

on:
  workflow_dispatch:
    inputs:
      skip_alerts:
        description: "Skip real-time alert checking"
        required: false
        default: "false"
      skip_insights:
        description: "Skip historical summary generation"
        required: false
        default: "false"

  schedule:
    # REAL-TIME ALERTS - every 15 minutes
    - cron: "*/15 * * * *"
    # HISTORICAL SUMMARIES - every 6 hours (00:00, 06:00, 12:00, 18:00 UTC)
    - cron: "0 */6 * * *"

env:
  NODE_VERSION: "18"
  SCRIPTS_PATH: "./scripts"

jobs:
  # ════════════════════════════════════════════════════════════════════════════
  # JOB 1: REAL-TIME ALERT MONITORING (Runs on EVERY schedule trigger)
  # ════════════════════════════════════════════════════════════════════════════
  monitor-alerts:
    name: 🚨 Real-Time Alert Monitoring
    runs-on: ubuntu-latest

    # FIXED: Run on any schedule OR manual trigger (unless skipped)
    if: |
      github.event_name == 'schedule' || 
      (github.event_name == 'workflow_dispatch' && github.event.inputs.skip_alerts != 'true')

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Cache Node Modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: ${{ env.SCRIPTS_PATH }}/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('scripts/package.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: 🔧 Install Dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: |
          echo "Installing dependencies..."
          npm install --production
          echo "Installation complete!"
        working-directory: ${{ env.SCRIPTS_PATH }}

      - name: 🔍 Verify Setup
        run: |
          echo "Verifying installation..."
          if [ ! -f "check-all-alerts.js" ]; then
            echo "❌ ERROR: check-all-alerts.js not found!"
            exit 1
          fi
          if [ ! -d "node_modules" ]; then
            echo "❌ ERROR: node_modules not found!"
            exit 1
          fi
          echo "✅ Setup verified"
        working-directory: ${{ env.SCRIPTS_PATH }}

      - name: 🚨 Check Thresholds (Email & SMS If Exceeded)
        id: check-alerts
        run: |
          echo "════════════════════════════════════════════════════════"
          echo "🚨 STARTING ALERT CHECK"
          echo "════════════════════════════════════════════════════════"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Run: #${{ github.run_number }}"
          echo ""

          node check-all-alerts.js 2>&1 | tee alert-output.log
          exit_code=${PIPESTATUS[0]}

          echo ""
          if [ $exit_code -ne 0 ]; then
            echo "════════════════════════════════════════════════════════"
            echo "❌ Script failed with exit code: $exit_code"
            echo "════════════════════════════════════════════════════════"
            cat alert-output.log
            exit $exit_code
          else
            echo "════════════════════════════════════════════════════════"
            echo "✅ Alert check completed successfully"
            echo "════════════════════════════════════════════════════════"
          fi
        working-directory: ${{ env.SCRIPTS_PATH }}
        env:
          FIREBASE_SERVICE_ACCOUNT_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}
          GMAIL_EMAIL: ${{ secrets.GMAIL_EMAIL }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          SEMAPHORE_API_KEY: ${{ secrets.SEMAPHORE_API_KEY }}
        continue-on-error: true

      - name: 📱 Send Push Notifications
        if: steps.check-alerts.outcome == 'success'
        run: |
          echo "📱 Preparing to send push notifications..."

          # Install Firebase Admin SDK
          npm install firebase-admin

          # Create notification script
          cat > send-notifications.js << 'EOF'
          const admin = require('firebase-admin');

          // Initialize Firebase Admin
          const serviceAccount = JSON.parse(process.env.FIREBASE_SERVICE_ACCOUNT_KEY);
          admin.initializeApp({
            credential: admin.credential.cert(serviceAccount),
            databaseURL: "https://weather-monitoring-syste-3c1ea-default-rtdb.asia-southeast1.firebasedatabase.app"
          });

          const db = admin.firestore();

          async function sendNotifications() {
            try {
              console.log('   🔍 Checking for recent alerts...');
              
              const alertsHistoryRef = db.collection('alerts_history').orderBy('timestamp', 'desc').limit(1);
              const snapshot = await alertsHistoryRef.get();
              
              if (snapshot.empty) {
                console.log('   ℹ️  No recent alerts found - no notifications to send');
                return;
              }
              
              const latestAlert = snapshot.docs[0].data();
              
              // Check if this alert was just created (within last 5 minutes)
              const alertTime = latestAlert.timestamp?.toDate();
              const now = new Date();
              const timeDiff = (now - alertTime) / 1000 / 60; // minutes
              
              if (timeDiff > 5) {
                console.log(`   ℹ️  Latest alert is ${Math.round(timeDiff)} minutes old - skipping`);
                return;
              }
              
              if (!latestAlert.alerts || latestAlert.alerts.length === 0) {
                console.log('   ℹ️  No threshold violations in latest check');
                return;
              }
              
              console.log(`   ⚠️  Found ${latestAlert.alerts.length} active alert(s)`);
              
              // Get FCM tokens from Firestore
              console.log('   📱 Fetching FCM tokens from Firestore...');
              const tokensSnapshot = await db.collection('fcm_tokens').get();
              
              if (tokensSnapshot.empty) {
                console.log('   ⚠️  No FCM tokens found in Firestore');
                console.log('   💡 Users need to enable notifications in the app');
                return;
              }
              
              const tokens = tokensSnapshot.docs.map(doc => doc.data().token);
              console.log(`   📱 Found ${tokens.length} registered device(s).`);
              
              // Prepare notification message
              const alertMessages = latestAlert.alerts.map(alert => 
                `${alert.metric || alert.parameter}: ${alert.value}${alert.unit || ''}`
              ).join(', ');
              
              const message = {
                notification: {
                  title: '🚨 Weather Alert',
                  body: alertMessages
                },
                data: {
                  alertCount: latestAlert.alerts.length.toString(),
                  timestamp: new Date().toISOString(),
                  link: '/alerts'
                },
                tokens: tokens
              };
              
              // Send to all devices
              console.log('   📤 Sending notifications...');
              const response = await admin.messaging().sendEachForMulticast(message);
              
              console.log(`   ✅ Successfully sent to ${response.successCount} device(s).`);
              
              if (response.failureCount > 0) {
                console.log(`   ⚠️  Failed to send to ${response.failureCount} device(s).`);
                response.responses.forEach((resp, idx) => {
                  if (!resp.success) {
                    console.log(`      Device ${idx + 1}: ${resp.error?.message}`);
                  }
                });
              }
              
            } catch (error) {
              console.error('   ❌ Error sending notifications:', error.message);
              console.log('   ℹ️  Continuing workflow despite notification error...');
            }
          }

          sendNotifications().then(() => {
            console.log('   ✅ Push notification process completed');
            process.exit(0);
          }).catch(err => {
            console.error('   ❌ Fatal error:', err);
            process.exit(0); // Don't fail the workflow
          });
          EOF

          # Run the notification script
          node send-notifications.js
        working-directory: ${{ env.SCRIPTS_PATH }}
        env:
          FIREBASE_SERVICE_ACCOUNT_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}
        continue-on-error: true

      - name: ✅ Alert Monitoring Success
        if: steps.check-alerts.outcome == 'success'
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "✅ Real-time alert monitoring completed!"
          echo "════════════════════════════════════════════════════════"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "✉️  Emails sent ONLY if thresholds were exceeded"
          echo "📱 Push notifications sent ONLY if thresholds were exceeded"
          echo "💬 SMS sent ONLY if enabled (via Semaphore - PH provider)"
          echo "📊 Check Firestore: alerts_history collection"
          echo "🔄 Next check: In 15 minutes"
          echo "════════════════════════════════════════════════════════"

      - name: ❌ Alert Monitoring Failed
        if: steps.check-alerts.outcome == 'failure'
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "❌ Real-time alert monitoring failed!"
          echo "════════════════════════════════════════════════════════"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "⚠️  CRITICAL: Alerts may not be sent!"
          echo "Check the logs above for error details"
          echo "════════════════════════════════════════════════════════"
          exit 1

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 2: HISTORICAL SUMMARY (Runs every 6 hours at :00 minutes)
  # ════════════════════════════════════════════════════════════════════════════
  generate-insights:
    name: 📊 Historical Summary Generation
    runs-on: ubuntu-latest

    # FIXED: Only run at specific times (when minute is 00) OR manual trigger
    # This runs at: 00:00, 06:00, 12:00, 18:00, etc. (every 6 hours)
    if: |
      (github.event_name == 'schedule' && 
       (github.run_number % 24 == 1 || github.run_number % 24 == 25)) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.skip_insights != 'true')

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🟢 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Cache Node Modules
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: ${{ env.SCRIPTS_PATH }}/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('scripts/package.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: 🔧 Install Dependencies
        if: steps.npm-cache.outputs.cache-hit != 'true'
        run: |
          echo "Installing dependencies..."
          npm install --production
          echo "Installation complete!"
        working-directory: ${{ env.SCRIPTS_PATH }}

      - name: 🔍 Verify Setup
        run: |
          echo "Verifying installation..."
          if [ ! -f "generate-summary.js" ]; then
            echo "❌ ERROR: generate-summary.js not found!"
            exit 1
          fi
          if [ ! -d "node_modules" ]; then
            echo "❌ ERROR: node_modules not found!"
            exit 1
          fi
          echo "✅ Setup verified"
        working-directory: ${{ env.SCRIPTS_PATH }}

      - name: 📈 Generate 24-Hour Weather Summary
        id: generate-insights
        run: |
          echo "════════════════════════════════════════════════════════"
          echo "📊 GENERATING HISTORICAL SUMMARY"
          echo "════════════════════════════════════════════════════════"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "Run: #${{ github.run_number }}"
          echo ""

          node generate-summary.js 2>&1 | tee summary-output.log
          exit_code=${PIPESTATUS[0]}

          echo ""
          if [ $exit_code -ne 0 ]; then
            echo "════════════════════════════════════════════════════════"
            echo "❌ Script failed with exit code: $exit_code"
            echo "════════════════════════════════════════════════════════"
            cat summary-output.log
            exit $exit_code
          else
            echo "════════════════════════════════════════════════════════"
            echo "✅ Summary generated successfully"
            echo "════════════════════════════════════════════════════════"
          fi
        working-directory: ${{ env.SCRIPTS_PATH }}
        env:
          FIREBASE_SERVICE_ACCOUNT_KEY: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}
        continue-on-error: true

      - name: ✅ Summary Generation Success
        if: steps.generate-insights.outcome == 'success'
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "✅ Historical weather summary generated!"
          echo "════════════════════════════════════════════════════════"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "📊 Analysis: Last 24 hours of sensor data"
          echo "💾 Saved to: insights/daily_prediction (Realtime DB)"
          echo "🖥️  Visible in: Dashboard 'Smart Insights' card"
          echo "🔄 Next update: In 6 hours"
          echo "════════════════════════════════════════════════════════"

      - name: ❌ Summary Generation Failed
        if: steps.generate-insights.outcome == 'failure'
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "❌ Historical summary generation failed!"
          echo "════════════════════════════════════════════════════════"
          echo "Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "ℹ️  Note: This is for dashboard display only"
          echo "✅ Real-time alerts still work independently"
          echo "Check the logs above for error details"
          echo "════════════════════════════════════════════════════════"
          exit 1

  # ════════════════════════════════════════════════════════════════════════════
  # JOB 3: SYSTEM HEALTH CHECK (Optional summary)
  # ════════════════════════════════════════════════════════════════════════════
  system-health-check:
    name: 🏥 System Health Check
    runs-on: ubuntu-latest
    needs: [monitor-alerts]
    if: always() && github.event_name == 'schedule'

    steps:
      - name: 📊 Generate Status Report
        run: |
          echo ""
          echo "════════════════════════════════════════════════════════"
          echo "🏥 WEATHER MONITORING SYSTEM - HEALTH REPORT"
          echo "════════════════════════════════════════════════════════"
          echo ""
          echo "Workflow Run: ${{ github.run_number }}"
          echo "Triggered by: ${{ github.event_name }}"
          echo "Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo ""
          echo "═══════════════════ JOB STATUS ═══════════════════════"
          echo "  🚨 Real-Time Alerts: ${{ needs.monitor-alerts.result }}"
          echo ""

          if [ "${{ needs.monitor-alerts.result }}" == "success" ]; then
            echo "✅ ALERT SYSTEM OPERATIONAL"
            echo ""
            echo "System Status:"
            echo "  • Alert monitoring: Active (every 15 min)"
            echo "  • Summary analysis: Active (every 6 hours)"
            echo "  • Email notifications: Configured"
            echo "  • Push notifications: Configured"
            echo "  • SMS notifications: Semaphore (PH provider)"
            echo ""
            echo "Next scheduled alert check: In 15 minutes"
          else
            echo "⚠️  ATTENTION REQUIRED"
            echo ""
            echo "❌ CRITICAL: Real-time alert system failed!"
            echo "   → Email/SMS/Push notifications may not be sent"
            echo "   → Immediate investigation required"
            echo ""
            echo "Troubleshooting checklist:"
            echo "  1. ✓ Firebase service account key is valid"
            echo "  2. ✓ Gmail credentials are correct"
            echo "  3. ✓ Semaphore API key is correct"
            echo "  4. ✓ Sensor is sending data regularly"
            echo "  5. ✓ Network connectivity is stable"
          fi
          echo ""
          echo "════════════════════════════════════════════════════════"
